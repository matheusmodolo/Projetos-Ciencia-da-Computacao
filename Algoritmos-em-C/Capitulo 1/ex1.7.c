#include <stdio.h>
#include <stdlib.h>
int main() {

    int l;
    int P;
    int A;

    printf("Valor do lado: ");
    scanf("%d", &l);

    P = 4 * l;
    A = l * l;

    printf("\nPerimetro = %d",P);
    printf("\nArea = %d",A);

return 0;

}
